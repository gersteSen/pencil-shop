import { NativeButtonDirective } from './native-button.directive';

describe('NativeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NativeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
